name : Building and pushing to AWS ECR

on:
  workflow_dispatch:

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo Message
        run : echo "Build and Push  successfully ✅"